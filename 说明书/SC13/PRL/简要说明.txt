旧控制逻辑：对应图纸 170523
	1、电机温度：喷液（默认80度）；温度过高报警（默认105度） 参数在“自定义参数”组中。
	2、油压过低报警：
	（1）压机启动油压差（在压机启动后5分钟内判断）。
 	油压 - 启动前油压 < [压机启动油压差]（默认3.0bar）时，开油泵，持续45秒报警。
	（2）油压过低。
  	压机启动后，油压 < [油压过低]（默认7.5bar）时，开油泵，持续45秒报警。

2017-08-01	对应图纸 170801
	1、修改油压：
	（1）并联时也是压机独立油压。（原来是1个并联系统1个油压）
	（2）油压保护方式改成：高压油压差过大(默认3.0bar) 和 油压低压差过小(默认2.5bar)。